import { DisplayCard } from "../../components/UI/DisplayCard";
type Props = {};

export const Calculator = (props: Props) => {
  return (
    <DisplayCard title="OBJECTIVE TIME">
      <hr className="m-auto h-1 bg-dark border-0 mb-3" />
    </DisplayCard>
  );
};
